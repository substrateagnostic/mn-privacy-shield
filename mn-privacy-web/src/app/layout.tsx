import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MN Privacy Shield - Exercise Your Minnesota Data Privacy Rights",
  description: "Free tool to help Minnesotans exercise their rights under the Minnesota Consumer Data Privacy Act (MCDPA). Generate request letters, track deadlines, and take control of your personal data.",
  keywords: ["Minnesota", "MCDPA", "data privacy", "consumer rights", "data broker", "opt-out", "delete my data"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
